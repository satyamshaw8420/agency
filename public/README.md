# Studio Public Assets

You can place your logo file in this folder:
- **SVG Format**: `/public/logo.svg`
- **PNG Format**: `/public/logo.png`

The website's header and footer are configured to automatically load your logo image from `/logo.svg` (or `/logo.png`).
